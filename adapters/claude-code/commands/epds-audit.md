---
description: Audit EPDS, AGENTS.md, CLAUDE.md, commands, skills, templates, tests, tools, and CI.
argument-hint: "[report|patch]"
---

Use the installed `epds` skill and execute the `audit` procedure.

Default to report-only. If patching is requested, show exact file changes and obtain approval before writing.
